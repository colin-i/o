
rm -f ./a.out
rm -f ./*.o
rm -f ./*.log
rm -f ./*.x

a=( `ls` )
for var in "${a[@]}"; do
	if [ -d "${var}" ]; then
		cd "${var}"
			rm -f ./*.o
			rm -f ./*.log
			rm -f ./*.x
			rm -f ./*.exe    #by ocomp
		cd ..
	fi
done
