
on:
 workflow_dispatch:
  inputs:
   tag:
    description: "project version"

jobs:
 delrel:
   runs-on: ubuntu-18.04
   #latest
   steps:
    - uses: cb80/delrel@latest
      with:
       tag: ${{ github.event.inputs.tag }}
       token: ${{ secrets.PAT }}
